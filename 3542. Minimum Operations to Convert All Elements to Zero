class Solution {
    public int minOperations(int[] nums) {
        int operations = 0;
        int currentLevel = 0;

        for (int num : nums) {
           
            if (num > currentLevel) {
                operations += (num - currentLevel);
            }
           
            currentLevel = num;
        }

        return operations;
    }
}
